package com.digiCart.notification_service.controller;

// Notification emails are now triggered via Kafka events consumed by NotificationEventConsumer.
// This controller is intentionally empty and can be removed.

spring:
  datasource:
    url: jdbc:h2:mem:pricedb;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  cloud:
    discovery:
      enabled: false

eureka:
  client:
    enabled: false

